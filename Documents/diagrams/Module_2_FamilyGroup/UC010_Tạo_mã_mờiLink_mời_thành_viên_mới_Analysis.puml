@startuml
title Analysis Class Diagram - UC010 GenerateInviteCode

class "InviteMemberUI" as UI <<boundary>> {
  + requestInviteToken() : void
  + displayInviteToken() : void
}
class "InviteController" as Controller <<control>> {
  + generateInvite(groupId: String) : void
}
class "FamilyDB" as Entity <<entity>> {
  + saveInviteToken(groupId: String, token: String, expiry: Date) : Boolean
}

Controller -- UI
Controller -- Entity
@enduml
